#*
A lambda function is a concise, single line, anonymous function 
useful for short, specific operations where defining a full function with def would be unnecessarily verbose
*#
with entry{
add = lambda b: float, c:float: b+c;

r: float = 20.0;
t: float = 234.0;

print(add(r,t));


}